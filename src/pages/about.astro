---
import LinkBtn from "@/components/atoms/LinkBtn.astro";
import PageLayout from "@/layouts/PageLayout.astro";
import { paragraphs, resume } from "@/content/data/about.json";
import { Image } from "astro:assets";
import favicon from "@/assets/favicon.jpg";
---

<PageLayout title="À propos" header="À propos">
  <div class="flex flex-col md:flex-row gap-6 items-center">
    <div class="flex flex-col items-center">
      <Image
        src={favicon}
        alt="Image de Timotheï Corneille"
        class="max-w-60 max-h-60 rounded-md"
      />
      <LinkBtn text="Voir mon CV" href={resume} target="_blank" />
    </div>
    <div>
      {
        paragraphs.map((paragraph) => (
          <>
            <p class="pb-3.5">{paragraph}</p>
          </>
        ))
      }
    </div>
  </div>
</PageLayout>
